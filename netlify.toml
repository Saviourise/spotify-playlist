[build]
  command = "npm run build"
  publish = "dist"

[functions]
  directory = "netlify/functions"
  node_bundler = "esbuild"

# Serve index.html for client side routes (function routes are handled first).
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
